<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>User Dashboard</title>
    <link rel="stylesheet" href="/css/user.css">
</head>
<body>
    <h1>Welcome, <span th:text="${user.userName}"></span>!</h1>
    <p>Email: <span th:text="${user.email}"></span></p>
    <p>Phone: <span th:text="${user.phoneNumber}"></span></p>
    <nav>
        <a href="/books">Browse Books</a>
        <a href="/user/borrow-history">My Borrow History</a>
        <a href="/logout">Logout</a>
    </nav>
</body>
</html>


<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Book List</title>
    <link rel="stylesheet" href="/css/catalog.css">
</head>
<body>
    <h1>Available Books</h1>
    <table>
        <tr>
            <th>Title</th>
            <th>Author</th>
            <th>Genre</th>
            <th>Availability</th>
            <th>Action</th>
        </tr>
        <tr th:each="book : ${books}">
            <td th:text="${book.bookName}"></td>
            <td th:text="${book.bookAuthor}"></td>
            <td th:text="${book.bookGenre}"></td>
            <td th:text="${book.availabilityStatus == 'Y' ? 'Available' : 'Not Available'}"></td>
            <td>
                <button th:if="${book.availabilityStatus == 'Y'}"
                        th:onclick="'borrowBook(' + ${book.bookName} + ')'">Borrow</button>
            </td>
        </tr>
    </table>
    <script src="/js/catalog.js"></script>
</body>
</html>


<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>My Borrow History</title>
    <link rel="stylesheet" href="/css/borrow.css">
</head>
<body>
    <h1>My Borrowed Books</h1>
    <table>
        <tr>
            <th>Book Name</th>
            <th>Borrow Date</th>
            <th>Due Date</th>
            <th>Status</th>
        </tr>
        <tr th:each="record : ${borrowHistory}">
            <td th:text="${record.bookId}"></td> <!-- Fetching bookId, modify if needed -->
            <td th:text="${record.borrowDate}"></td>
            <td th:text="${record.dueDate}"></td>
            <td th:text="${record.returnStatus ? 'Returned' : 'Not Returned'}"></td>
        </tr>
    </table>
</body>
</html>


<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Borrow Book</title>
    <link rel="stylesheet" href="/css/borrow.css">
</head>
<body>
    <h1>Borrow a Book</h1>
    <form action="/borrow-records" method="post">
        <label>User ID:</label>
        <input type="number" name="userId" required>
        <label>Book ID:</label>
        <input type="number" name="bookId" required>
        <label>Borrow Date:</label>
        <input type="date" name="borrowDate" required>
        <label>Due Date:</label>
        <input type="date" name="dueDate" required>
        <button type="submit">Borrow</button>
    </form>
</body>
</html>


function borrowBook(bookId) {
    fetch(`/borrow-records`, {
        method: "POST",
        headers: {
            "Content-Type": "application/json"
        },
        body: JSON.stringify({
            userId: 1,  // Replace with actual user ID
            bookId: bookId,
            borrowDate: new Date().toISOString().split('T')[0],
            dueDate: new Date(Date.now() + 7 * 24 * 60 * 60 * 1000).toISOString().split('T')[0],
            returnStatus: false
        })
    }).then(response => response.json())
      .then(data => alert("Book Borrowed Successfully!"))
      .catch(error => console.error("Error:", error));
}

document.addEventListener("DOMContentLoaded", function () {
    console.log("Borrow History Loaded");
});
S
